开启前提
en
conf t
no ip domain-lo
lin con 0 
exec-t 0 0
logg syn
exit
ho 

配置route
ip route IP网段地址 掩码 下一跳 优先级(可填浮动route)

配置trunk接口
int r f0/N-M
sw m t

配置vtp(前提配置trunk接口)
vtp dom 名字
vtp mo s\c\t
vl d
vl N

二层交换机配置子接口
int f0/0.N
en do (vl口) na(是ping交换机)
ip add 地址

路由器配置dhcp地址池
ip dh po (vl口)       ip dh ex ip地址 ip地址
de 网关地址
ne  ip地址 掩码
dns ip地址

远程控制
li vty ?  (问他有多少接口)
pass 123
login
ena se 123

ftp/tftp备份到服务器
sh fla
copy fla tftp
cop fl f

网络多条链路 待更新
